Flask==2.3.2
Flask-CORS
python-dotenv==1.0.0
openai==1.3.0

# Original ML libraries (preserved for accuracy)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu
torchaudio==2.3.1+cpu

# Original sentence transformer (preserved for accuracy)
sentence-transformers==2.7.0

# FAISS for vector search (CPU-only)
faiss-cpu==1.8.0

# HTTP client with proxy support
httpx==0.27.0

# Core dependencies
numpy==1.26.4
requests==2.32.3
Werkzeug==2.3.7

# Disable telemetry
huggingface-hub==0.21.4

# spaCy for NLP (original dependency)
spacy==3.7.2
