# V1.6.5 Alignment & V1.6.6 Multi-Course Development Plan

## 🎯 **Your Excellent Strategy**

### **Step 1: Ensure V1.6.5 Alignment** ✅
- Verify current API server, course files, and query engine are 100% aligned
- Disable multi-course functionality temporarily
- Ensure V1.6.5 performance is preserved

### **Step 2: Create GPTTutor_general** ✅
- Duplicate current GPTTutor_Decision to GPTTutor_general
- Work on V1.6.6 multi-course refactoring in the new folder
- Keep V1.6.5 decision version stable and working

## 📋 **Step 1: V1.6.5 Alignment Verification**

### **1.1 Current State Analysis**
- ✅ **Glossary Alignment**: Already completed via `direct_glossary_alignment.py`
- ✅ **API Server**: Using `courses/decision/glossary.json`
- ✅ **Query Engine**: Internal `CONCEPT_GLOSSARY` aligned
- ✅ **Course Config**: `courses/decision/course_config.json` created

### **1.2 Disable Multi-Course Functionality**
Modify `api_server.py` to temporarily disable multi-course:

```python
# In api_server.py - TEMPORARY DISABLE MULTI-COURSE
def load_course_config(course_id: str) -> dict:
    """
    TEMPORARY: Always use decision course for V1.6.5 stability
    """
    # Force decision course for V1.6.5
    course_id = "decision"
    
    course_path = os.path.join(COURSES_DIR, course_id)
    config = {
        "course_id": course_id,
        "glossary": {},
        "prompt_template": "",
        "sections_config": {}
    }
    
    # Load glossary.json
    glossary_path = os.path.join(course_path, "glossary.json")
    if os.path.exists(glossary_path):
        try:
            with open(glossary_path, 'r', encoding='utf-8') as f:
                config["glossary"] = json.load(f)
            print(f"✅ Loaded glossary for course '{course_id}'")
        except Exception as e:
            print(f"❌ Failed to load glossary for course '{course_id}': {e}")
    
    return config
```

### **1.3 Verify Alignment**
Create alignment verification script:

```python
# verify_v165_alignment.py
def verify_v165_alignment():
    """Verify V1.6.5 components are fully aligned"""
    
    # 1. Test API server with decision course
    # 2. Test direct query engine
    # 3. Compare outputs for same query
    # 4. Verify glossary consistency
    # 5. Test performance matches expectations
```

## 📋 **Step 2: Create GPTTutor_general**

### **2.1 Directory Structure**
```
GPTTutor_general/
├── api_server.py (V1.6.6 multi-course ready)
├── query_engine.py (course-agnostic)
├── course_config_loader.py
├── courses/
│   ├── decision/
│   │   ├── course_config.json
│   │   ├── glossary.json
│   │   └── ...
│   └── marketing/ (future)
├── tests/
└── docs/
```

### **2.2 V1.6.6 Development Plan**

#### **Phase 1: Course-Agnostic Query Engine**
```python
# query_engine.py (V1.6.6)
def detect_course_concept_domains(query: str, course_config: dict) -> dict:
    """Course-agnostic domain detection"""
    query_lower = query.lower()
    domains = {}
    
    for domain_name in course_config['domains']:
        domains[domain_name] = 0
    
    for domain_name, domain_config in course_config['domains'].items():
        score = 0
        for keyword in domain_config['keywords']:
            if keyword in query_lower:
                score += 1
        domains[domain_name] = score
    
    return domains

def extract_application_field(query: str, course_config: dict) -> str:
    """Course-agnostic application field extraction"""
    query_lower = query.lower()
    application_fields = course_config['application_fields']
    
    for field in application_fields:
        if field.replace('_', ' ') in query_lower:
            return field
    
    return application_fields[0]

def process_query(query: str, course_config: dict = None) -> str:
    """Main query processing - course-agnostic"""
    if course_config is None:
        course_config = load_course_config("decision")
    
    domains = detect_course_concept_domains(query, course_config)
    application_field = extract_application_field(query, course_config)
    # ... rest of processing
```

#### **Phase 2: Enhanced API Server**
```python
# api_server.py (V1.6.6)
def load_course_config(course_id: str) -> dict:
    """Load complete course configuration"""
    try:
        # Load main course config
        config = load_course_config_from_file(course_id)
        
        # Load additional components
        config['glossary'] = load_course_glossary(course_id)
        config['prompt_template'] = load_course_prompt_template(course_id)
        config['sections_config'] = load_course_sections_config(course_id)
        
        return config
    except Exception as e:
        print(f"❌ Failed to load course config for '{course_id}': {e}")
        return load_course_config("decision")
```

## 📋 **Implementation Timeline**

### **Week 1: V1.6.5 Stabilization**
- [ ] **Day 1-2**: Verify V1.6.5 alignment
- [ ] **Day 3**: Disable multi-course functionality
- [ ] **Day 4-5**: Test V1.6.5 performance and stability

### **Week 2: V1.6.6 Foundation**
- [ ] **Day 1**: Create GPTTutor_general directory
- [ ] **Day 2-3**: Refactor domain detection (course-agnostic)
- [ ] **Day 4-5**: Refactor application field extraction

### **Week 3: V1.6.6 Completion**
- [ ] **Day 1-2**: Refactor entity extraction
- [ ] **Day 3-4**: Update API server for multi-course
- [ ] **Day 5**: Testing and validation

## 🎯 **Benefits of This Approach**

### **✅ V1.6.5 Stability**
- Preserves working decision course functionality
- No risk of breaking current performance
- Can continue using V1.6.5 while developing V1.6.6

### **✅ V1.6.6 Development**
- Clean slate for multi-course architecture
- No constraints from existing hardcoded logic
- Can implement true course-agnostic design

### **✅ Risk Mitigation**
- If V1.6.6 has issues, V1.6.5 remains stable
- Can roll back to V1.6.5 at any time
- Gradual migration path

## 🚀 **Next Steps**

1. **Verify GPTTutor_general folder creation**
2. **Implement V1.6.5 alignment verification**
3. **Disable multi-course functionality in V1.6.5**
4. **Begin V1.6.6 development in GPTTutor_general**

This approach ensures we maintain a stable, working V1.6.5 while building the true multi-course V1.6.6 system. 