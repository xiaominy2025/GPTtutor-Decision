from query_engine import process_query

# Test the tariff uncertainty question
result = process_query("Under tarrif uncertainty, how do I plan my production?")

print("=== TARIFF UNCERTAINTY QUERY RESULT ===")
print(result)
print("=== END RESULT ===") 