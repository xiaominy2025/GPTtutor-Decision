Flask==2.3.2
Flask-CORS
python-dotenv==1.0.0
openai==0.28

# Optimized ML libraries for Lambda
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu
torchaudio==2.3.1+cpu

# Smaller, Lambda-friendly sentence transformer
sentence-transformers==2.7.0

# FAISS for vector search (CPU-only)
faiss-cpu==1.8.0

# HTTP client with proxy support
httpx==0.27.0

# Core dependencies
numpy==1.26.4
requests==2.32.3
Werkzeug==2.3.7

# Disable telemetry
huggingface-hub==0.21.4
